spring:
  datasource:
    url: jdbc:mysql://localhost:3306/flightapi?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: QWE123asd
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # 使用 schema.sql 建表
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type.descriptor.sql.BasicBinder: trace

  sql:
    init:
      mode: never     # 启动时不执行 schema.sql 和 data.sql

  messages:
    basename: i18n/messages
    encoding: UTF-8

server:
  port: 8080

app:
  jwt:
    secret: YmFzZTY0ZW5jb2RlZHNlY3JldGtleWZvcnRlc3Rpbmc=    #用Base64 编码的密钥, 更安全
    expiration-ms: 1800000   # 30分钟，建议10-30分钟之内